zbox_unified_push_bundle_api:
    resource:     "@ZboxUnifiedPushBundle/Resources/config/routing_api.yml"
    type:         rest
    prefix:       api/rest/{version}
    requirements:
        version:  latest|v1
        _format:  json
    defaults:
        version:  latest
